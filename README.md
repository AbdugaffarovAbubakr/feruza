# Feya Bot

1. Copy .env.example to .env and fill BOT_TOKEN and ADMIN_IDS.
2. Install dependencies: npm install
3. Start the bot: npm start

Notes
- Add the bot as admin in required channels so membership checks work.
- Admin IDs are numeric Telegram user IDs separated by comma.
